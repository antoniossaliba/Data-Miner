<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Data Miner</title>
    <link
      href="https://fonts.googleapis.com/css?family=Inter:400,600&display=swap"
      rel="stylesheet"
    />
    <style>
      body {
        font-family: "Inter", Arial, sans-serif;
        margin: 0;
        background: linear-gradient(120deg, #e0e7ff 0%, #f9f9f9 100%);
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
      }
      .container {
        max-width: 420px;
        width: 95vw;
        margin: 40px auto;
        background: rgba(255, 255, 255, 0.95);
        padding: 48px 32px 36px 32px;
        border-radius: 22px;
        box-shadow: 0 8px 32px rgba(60, 60, 120, 0.16), 0 1.5px 8px #b8c6ff33;
        display: flex;
        flex-direction: column;
        align-items: center;
        position: relative;
        overflow: hidden;
      }
      .container::before {
        content: "";
        position: absolute;
        top: -60px;
        right: -60px;
        width: 160px;
        height: 160px;
        background: radial-gradient(circle, #6b8cff33 60%, transparent 100%);
        z-index: 0;
      }
      .container::after {
        content: "";
        position: absolute;
        bottom: -60px;
        left: -60px;
        width: 160px;
        height: 160px;
        background: radial-gradient(circle, #b8c6ff33 60%, transparent 100%);
        z-index: 0;
      }
      h1 {
        font-size: 2.3em;
        font-weight: 700;
        color: #2d3a5a;
        margin-bottom: 10px;
        letter-spacing: -1.5px;
        z-index: 1;
        text-shadow: 0 2px 8px #e0e7ff;
      }
      .subtitle {
        color: #6b7a99;
        font-size: 1.08em;
        margin-bottom: 28px;
        z-index: 1;
        text-align: center;
      }
      label {
        font-size: 1.09em;
        color: #3d4a6a;
        margin-bottom: 10px;
        display: block;
        font-weight: 500;
        z-index: 1;
      }
      .input-group {
        width: 100%;
        display: flex;
        gap: 0.5em;
        margin-bottom: 18px;
        z-index: 1;
      }
      input[type="url"] {
        flex: 1;
        padding: 13px 15px;
        font-size: 1.07em;
        border: 1.7px solid #d3d8e8;
        border-radius: 8px;
        outline: none;
        background: #f7faff;
        transition: border 0.2s, box-shadow 0.2s;
        box-shadow: 0 1px 4px #e0e7ff44;
      }
      input[type="url"]:focus {
        border: 1.7px solid #6b8cff;
        background: #f0f4ff;
        box-shadow: 0 2px 8px #b8c6ff33;
      }
      button {
        padding: 13px 22px;
        font-size: 1.09em;
        font-weight: 600;
        color: #fff;
        background: linear-gradient(90deg, #6b8cff 0%, #4f6edb 100%);
        border: none;
        border-radius: 8px;
        cursor: pointer;
        box-shadow: 0 2px 8px rgba(60, 60, 120, 0.1);
        transition: background 0.2s, transform 0.1s, box-shadow 0.2s;
        z-index: 1;
      }
      button:hover,
      button:focus {
        background: linear-gradient(90deg, #4f6edb 0%, #6b8cff 100%);
        transform: translateY(-2px) scale(1.04);
        box-shadow: 0 4px 16px #b8c6ff44;
      }
      .info {
        margin-top: 32px;
        color: #6b7a99;
        font-size: 1.07em;
        background: #f0f4ff;
        padding: 20px 18px;
        border-radius: 10px;
        box-shadow: 0 1px 6px #e0e7ff44;
        text-align: center;
        z-index: 1;
      }
      .info span {
        font-size: 0.97em;
        color: #b0b8d0;
        display: block;
        margin-top: 8px;
      }
      @media (max-width: 600px) {
        .container {
          padding: 22px 4vw 18px 4vw;
        }
        h1 {
          font-size: 1.3em;
        }
        .subtitle {
          font-size: 0.98em;
        }
        .info {
          font-size: 0.97em;
        }
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1>Data Miner</h1>
      <div class="subtitle">
        Extract clean JSON data from any website for LLM training.
      </div>
      <form id="urlForm" autocomplete="off" method="POST" action="/extract">
        <label for="urlInput">Enter a URL:</label>
        <div class="input-group">
          <input
            type="url"
            id="urlInput"
            name="url"
            placeholder="https://example.com"
            required
          />
          <button type="submit">Extract JSON</button>
        </div>
      </form>
      <div class="info">
        Paste any website URL above and click <b>Extract JSON</b>.<br />
        You’ll get a JSON file ready for LLM training.
      </div>
    </div>
  </body>
</html>
